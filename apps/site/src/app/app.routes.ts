import { Route } from '@angular/router';

/**
 * Sintezaur site routes. Empty for M0 — only the root path is rendered
 * via the App component's template ("Hello Sintezaur"). Section routes
 * (`/tezaur`, `/bazar`, `/revista`, `/forum`) land in M2/M3/M4/M5.
 */
export const appRoutes: Route[] = [];
